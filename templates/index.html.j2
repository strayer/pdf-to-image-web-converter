<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">

    <title>PDF zu Bild</title>

    <!-- TODO: favicon? -->
    <link rel="icon" href="data:,">
  </head>
  <body>
    <div class="container">
      {% for message in get_flashed_messages() %}
        <div class="alert alert-primary">{{ message }}</div>
      {% endfor %}

      <div class="row mb-1">
        <div class="col">
          <h1 class="display-1">PDF-Datei in Bild umwandeln</h1>
        </div>
      </div>

      <div class="row mb-3">
        <div class="col">
          <p class="lead">Um ein PDF in ein Bild zu konvertieren, wähle die zu konvertierende Datei unten aus und klicke auf "Konvertieren".</p>
        </div>
      </div>

      <form method="post" enctype="multipart/form-data" action="{{ url_for('upload') }}" onsubmit="this.submit.disabled = true">
        <div class="row gy-3" style="max-width: 970px;">
          <label class="col-auto col-form-label" for="file">PDF-Datei auswählen</label>
          <div class="col-auto col-md">
            <input id="file" name="file" class="form-control" type="file" required accept="application/pdf">
          </div>
          <div class="col-auto">
            <button class="btn btn-primary w-100" type="submit">Konvertieren</button>
          </div>
        </div>
      </form>
    </div>

    <script>
      window.addEventListener("unload", function() {
        document.querySelector("button[type=submit]").disabled = false;
      });
    </script>
  </body>
</html>
